RIM_ENABLE_HOSTEXEC=0
if [ -n "$RUNIMAGE" ]
    then
        RIM_OVERFS_MODE=1
        RIM_KEEP_OVERFS=0
        RIM_OVERFS_ID="upd$(date +"%H%M%S").$RUNPID"
    else
        RIM_OVERFS_MODE=0
        unset RIM_OVERFS_ID RIM_KEEP_OVERFS
fi
